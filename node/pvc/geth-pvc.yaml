apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: geth-pvc
  labels:
    app: validator
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ssd-rwo
  resources:
    limits:
      storage: 2000Gi 
    requests: 
      storage: 2000Gi 
